package com.mailmanager.repository;

import com.mailmanager.domain.ProxyBinding;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProxyBindingRepository extends JpaRepository<ProxyBinding, Long> {
}
